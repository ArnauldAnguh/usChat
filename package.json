{
  "name": "uschat",
  "version": "1.0.0",
  "description": "a simple live chatroom app",
  "engines": {
    "npm": "6.12.0",
    "node": "12.13.0"
  },
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && mkdir dist && babel src -s -d dist",
    "prod": "npm run build && node dist/index"
  },
  "author": "Arnauld Anguh",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.2",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.6.4",
    "@babel/node": "^7.0.0",
    "@babel/plugin-transform-runtime": "^7.0.0",
    "@babel/polyfill": "^7.0.0",
    "@babel/preset-env": "^7.6.3",
    "@babel/register": "^7.0.0",
    "babel-watch": "^7.0.0"
  }
}